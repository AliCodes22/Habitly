import Card from "@material-ui/core/Card";
import CardHeader from "@material-ui/core/CardHeader";
import CardContent from "@material-ui/core";

const HabitCard = ({ note }) => {
  return <div>HabitCard</div>;
};

export default HabitCard;
